<%= render 'base' %>
<h1><%= t("activerecord.models.feedback") %></h1>
<div id="admin_feedbacks">
  <table class="table table-bordered table-striped table-condensed">
    <tr class="head">
      <td>编号</td>
      <td><%= t("activerecord.attributes.feedback.name") %></td>
      <td><%= t("activerecord.attributes.feedback.tel") %></td>
      <td><%= t("activerecord.attributes.feedback.qq") %></td>
      <td><%= t("activerecord.attributes.feedback.title") %></td>
      <td><%= t("activerecord.attributes.feedback.content") %></td>
      <td>创建时间</td>
      <td class="opts"></td>
    </tr>
    <% @feedbacks.each do |item| %>
    <tr class="<%= cycle("","even") %>">
      <td><%= item.id %></td>
      <td><%= item.name %></td>
      <td><%= item.tel %></td>
      <td><%= item.qq %></td>
      <td><%= item.title %></td>
      <td><%= item.content %></td>
      <td><%= l(item.created_at, :format => :long) %></td>
      <td>
        <%= link_to "查看", admin_feedback_path(item.id), :class => "icon small_edit" %>
        <%= link_to "编辑", edit_admin_feedback_path(item.id), :class => "icon small_edit" %>
        <%= link_to "删除", admin_feedback_path(item.id), :method => :delete, 'data-confirm' => '确定要删除吗?', :class => "icon small_delete" %>
      </td>
    </tr>
    <% end %>
  </table>
</div>
<div class="tools">
  <a href="<%= new_admin_feedback_path %>" class="btn btn-small btn-success">新建</a>
</div>
